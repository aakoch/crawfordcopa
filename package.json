{
  "name": "crawfordcopa",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "parcel": "^2.11.0",
    "pug": "^3.0.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --no-optimize --no-scope-hoist --log-level verbose src/index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-pug": "^2.11.0"
  }
}
